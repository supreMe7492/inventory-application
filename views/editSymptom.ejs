<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/styles/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Symptoms</title>
</head>
<body>
    <a href="/add_symptom" class="nav-btn">Add new symptom</a>
    <section>
    <% symptoms.forEach(symptom => {%>
        <div class="symptom-row">
            <span><%=symptom.symptom_name%></span>
            <form action="/edit_symptom/delete_symptom/<%=symptom.symptom_id%>" method="post">
                <button type="submit" class="delete-btn">X</button>
            </form>
        </div>
    <%})%>  
    </section>
</body>
</html>