<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/styles/card.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>poisions-inventory</title>
</head>
<body>
    <% poisons.forEach(poison => { %>
  <div class="poison-card">
    <h2><%= poison.poisionName %></h2>
    <p><strong>Toxin Type:</strong> <%= poison.toxin_type %></p>
    <p><strong>Lethality Level:</strong> <%= poison.lethality_level %></p>
    <p><strong>Symptoms:</strong> <%= poison.symptoms.map(s => s.symptom_name).join(', ') %></p>    
    <a href="/edit/<%=poison.id%>" class="edit-btn">Edit</a>
    <button class="delete-btn">Delete</button>
  </div>
<% }) %>
</body>
</html>